
<script src="~/js/app.js/OSVMView.js"></script>



<div class="right_col" role="main">
    <div class="page-title">
        <div class="row">
            <div class="col-md-11">
                <div class="title_left">
                    <h3>OSVM</h3>
                </div>
            </div>
        </div>
    </div>
    <div class="clearfix"></div>

    <div class="row">
        <div class="col-md-12 col-sm-12 col-xs-12">
            <div class="x_panel">
                <div class="x_content">
                    <div class="form-group" id="gridContainer">
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


@*<script src="~/lib/devextreme-aspnet-data/js/dx.aspnet.data.js"></script>
    <script src="~/lib/devextreme/js/dx.aspnet.mvc.js"></script>
    <script>
        function formfiled() {

        }
    </script>
    <div class="right_col" role="main">
        <div class="page-title">
            <div class="title_left">
                <h3>OSVM</h3>
            </div>
        </div>
        <div class="clearfix"></div>
        <div class="row">

            <div class="col-md-12 col-sm-12 col-xs-12">
                <div class="x_panel">
                    <div class="x_content">
                        @(Html.DevExtreme().DataGrid<WebdocTerminal.Models.OSVM>()
                            .ID("OSVMId")
                            .WordWrapEnabled(true)
                            .FilterRow(filterRow => filterRow
                            .Visible(true)
                            .ApplyFilter(GridApplyFilterMode.Auto))
                            .Paging(paging => paging.PageSize(10))
                            .SearchPanel(searchPanel => searchPanel
                                    .Visible(true)
                                    .Width(240)
                                    .Placeholder("Search...")
                                ).Editing(editing =>
                                {
                                    editing.Mode(GridEditMode.Popup);
                                    editing.AllowAdding(false);
                                    editing.AllowDeleting(true);
                                    editing.AllowUpdating(true);
                                    editing.UseIcons(true);
                                    editing.Popup(p => p
                                    .ShowTitle(false)
                                    .Width(700)
                                    .Height(345)
                                    .Position(pos => pos
                                    .My(HorizontalAlignment.Center, VerticalAlignment.Center)
                                    .At(HorizontalAlignment.Center, VerticalAlignment.Center)
                                    .Of(new JS("window"))
                                    )
                                    );
                                })
                            .Columns(c =>
                            {
                                c.Add().DataField("containerNo");
                                c.Add().DataField("pccssSealNumber");
                                c.Add().DataField("bondedCarrierName");
                                c.Add().DataField("vehicleNo");
                                c.Add().DataField("gdNumber");
                                //c.Add().DataField("gdNumber").ValidationRules(v => v.AddRequired());

                            })
                            .DataSource(d => d.Mvc()
                            .Controller("OSVM")
                            .LoadAction("GetOSVMs")
                            .InsertAction("Post")
                            .UpdateAction("Put")
                            .DeleteAction("Delete")
                            .Key("osvmId"))
                        )

                    </div>
                </div>
            </div>

        </div>

    </div>*@

